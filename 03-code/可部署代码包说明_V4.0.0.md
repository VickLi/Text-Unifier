# Text Unifier V4.0 无缺陷可部署代码包

> **版本**: V4.0.0
> **状态**: ✅ 已通过全维度测试验证，满足所有终止阈值
> **测试日期**: 2026-05-22
> **测试报告**: `04-test/测试报告_V4.0.0.md`
> **Bug 报告**: `04-test/Bug报告_V4.0.0.md`

## 项目结构（03-code/）

```
03-code/
├── src/                        # React 前端
│   ├── components/             # 13 个 UI 组件
│   │   ├── CleanPanel.tsx      # 内容清洗 + SearchReplace
│   │   ├── ConnectionPointItem.tsx  # 连接点条目
│   │   ├── ConnectionPointList.tsx  # 连接点列表/搜索结果
│   │   ├── DiffViewer.tsx      # 文档对比
│   │   ├── DragOverlay.tsx     # 拖拽遮罩
│   │   ├── ExportButton.tsx    # 导出（连续文本）
│   │   ├── FileChipBar.tsx     # 文件芯片栏
│   │   ├── Minimap.tsx         # 缩略图
│   │   ├── ModeTabs.tsx        # 模式切换
│   │   ├── PreviewPanel.tsx    # 连续文本编辑器
│   │   ├── SortableChip.tsx    # 可拖拽芯片
│   │   ├── ToggleButton.tsx    # 双向切换按钮
│   │   └── Tooltip.tsx         # 提示框
│   ├── store/useStore.ts       # Zustand（28字段 30动作）
│   ├── types/index.ts          # V4.0 类型定义
│   ├── utils/
│   │   ├── ipc.ts              # 4通道 IPC（mergeFiles/encoding/export/select）
│   │   ├── cjkConv.ts          # 繁简转换引擎
│   │   └── diffUtils.ts        # 文档对比算法
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── electron/
│   ├── main.ts                 # 4 IPC handler
│   ├── preload.ts              # contextBridge
│   └── preload.d.ts
├── native/
│   ├── src/
│   │   ├── lib.rs              # 2 napi 导出
│   │   ├── text_normalizer.rs  # 文本归一化
│   │   ├── encoding_detector.rs # 编码探测链
│   │   └── chain_merger.rs     # 链式重叠合并
│   ├── Cargo.toml
│   └── build.rs
├── package.json                # v4.0.0
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── electron-builder.yml
```

## 启动

```powershell
cd 03-code
npm install
cd native && napi build --platform --release && cd ..
npm run electron:dev     # 开发模式
npm run electron:build   # 生产打包
```

## 技术栈

| 层 | 组件 | 版本 |
| :--- | :--- | :--- |
| 桌面 | Electron | 31 |
| 前端 | React + TypeScript | 18.3 / 5.4 |
| 状态 | Zustand | 4.5 |
| 样式 | Tailwind CSS | 3.4 |
| 引擎 | Rust napi-rs | 2021 |
| 打包 | electron-builder | 25 |
