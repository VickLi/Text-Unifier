# 项目隐私安全审查报告

**项目名称**: Text Unifier  
**版本**: v3.2.2 (精简后)  
**审查日期**: 2026-05-16  
**报告目的**: 在项目推送到 GitHub 公开仓库前，全面审查代码、配置文件、脚本和文档中是否包含隐私信息、敏感凭证或不应公开的开发环境信息。

---

## 目录

1. [检查范围与方法](#1-检查范围与方法)
2. [已发现的隐私问题](#2-已发现的隐私问题)
3. [检查通过的项目](#3-检查通过的项目)
4. [修复建议汇总](#4-修复建议汇总)
5. [附录：AI 隐私检查提示词](#5-附录ai-隐私检查提示词)

---

## 1. 检查范围与方法

| 检查项 | 扫描内容 | 方法 |
|:-------|:---------|:-----|
| **身份信息** | 真实姓名、邮箱地址 | 正则搜索 + 手动校验 |
| **API 密钥 / Token** | `api_key`, `secret`, `token`, `password` 等 | 关键词搜索 |
| **私钥 / 证书** | `.pem`, `.key`, `.crt`, `.p12` 文件 | 文件扩展名扫描 |
| **绝对路径** | 本地开发路径（如 `C:\...`, `G:\...`） | 关键词搜索 |
| **开发环境配置** | MSVC 版本、SDK 版本、工具链路径 | 手动校验 |
| **数据库信息** | 连接字符串、数据库密码 | 关键词搜索 |
| **Git 敏感文件** | `.git-rewrite/`, 未忽略的本地配置 | 目录扫描 |
| **配置文件** | package.json, .gitignore, 各 config 文件 | 手动校验 |
| **文档内容** | docs/ 目录、README.md | 手动校验 |

---

## 2. 已发现的隐私问题

### 🔴 2.1 高风险

#### 2.1.1 脚本中包含真实姓名与邮箱

| 属性 | 说明 |
|:-----|:------|
| **风险等级** | 🔴 **高风险** |
| **泄露类型** | 真实身份信息（姓名 + 个人邮箱） |
| **涉及文件** | `scripts/rebuild-git-history.ps1` (L12-L13) |
| | `scripts/build-git-history.ps1` (L4, L12-L13) |
| **泄露内容** | 真实姓名 `VickLi` 和个人邮箱 `hbxgswgr@gmail.com` |
| **风险说明** | 脚本中 `git config user.name` 和 `git config user.email` 直接暴露了开发者的真实姓名和邮箱地址。公开仓库中此信息可被爬取，导致垃圾邮件、社交工程攻击等风险。 |
| **修复建议** | 将真实姓名和邮箱替换为占位符或通用值，如 `git config user.name "Developer"`、`git config user.email "dev@example.com"` |

---

### 🟡 2.2 中风险

#### 2.2.1 脚本中包含绝对路径

| 属性 | 说明 |
|:-----|:------|
| **风险等级** | 🟡 **中风险** |
| **泄露类型** | 本地开发目录结构 |
| **涉及文件** | `scripts/rebuild-git-history.ps1` (L5) |
| | `scripts/build-git-history.ps1` (L5) |
| | `scripts/启动开发环境.ps1` (L25) |
| **泄露内容** | `G:\CodeProject\Text Unifier` |
| **风险说明** | 暴露了开发者本地磁盘分区、项目组织方式和工作目录结构。攻击者可通过此信息推断开发环境特征，辅助针对性攻击。 |
| **修复建议** | 替换为相对路径或环境变量，如 `$projectRoot = Split-Path $PSScriptRoot`、`cd $projectRoot` |

#### 2.2.2 开发环境工具链版本暴露

| 属性 | 说明 |
|:-----|:------|
| **风险等级** | 🟡 **中风险** |
| **泄露类型** | 编译器版本、SDK 版本、工具链路径 |
| **涉及文件** | `scripts/启动开发环境.ps1` (L10-L17) |
| **泄露内容** | MSVC 版本 `14.44.35207`、Windows SDK 版本 `10.0.26100.0`、SDK 安装路径 `C:\Program Files (x86)\Windows Kits\10` |
| **风险说明** | 暴露了具体的编译工具链版本和操作系统 SDK 版本。虽风险较低，但结合其他信息可精确锁定开发环境配置。 |
| **修复建议** | 使用环境变量或通配符自动检测 MSVC/SDK 版本（如 `Get-ChildItem` 动态查找最新版本），而非硬编码版本号 |

---

### 🟢 2.3 低风险

#### 2.3.1 Git 配置包含远程仓库信息

| 属性 | 说明 |
|:-----|:------|
| **风险等级** | 🟢 **低风险** |
| **泄露类型** | GitHub 仓库地址 |
| **涉及文件** | `.git/config` |
| **泄露内容** | `url = https://github.com/VickLi/Text-Unifier.git` |
| **风险说明** | 公开仓库地址本身可能已公开，但结合脚本中的姓名可确认对应关系。 |
| **修复建议** | 使用非个人信息用户名创建组织仓库，或接受此风险（仓库本身已公开） |

#### 2.3.2 `.git-rewrite/` 目录未彻底管理

| 属性 | 说明 |
|:-----|:------|
| **风险等级** | 🟢 **低风险** |
| **泄露类型** | Git 历史重写备份 |
| **涉及文件** | `.git-rewrite/` 目录 |
| **当前状态** | 已在 `.gitignore` 中忽略 |
| **风险说明** | `.git-rewrite/` 目录包含 `git filter-repo` 等历史重写工具的备份数据。虽已加入 `.gitignore`，但需要确认该目录中是否包含已删除的敏感文件副本。 |
| **修复建议** | 确认 `.git-rewrite/` 已从 Git 跟踪中移除且从未被提交过 |

---

## 3. 检查通过的项目

| 检查项 | 结果 | 说明 |
|:-------|:----:|:------|
| API 密钥 / Token | ✅ **通过** | 未发现硬编码的 API 密钥、Token 或 Secret |
| 私钥 / 证书文件 | ✅ **通过** | 未发现 `.pem`、`.key`、`.crt`、`.p12` 等敏感证书文件 |
| 数据库连接字符串 | ✅ **通过** | 未发现任何数据库连接信息 |
| 密码硬编码 | ✅ **通过** | 代码中无任何显式密码 |
| 邮箱地址（代码中） | ✅ **通过** | 除脚本中的开发者邮箱外，代码逻辑中无其他邮箱 |
| 内网 IP 地址 | ✅ **通过** | 未发现 `192.168.x.x`、`10.x.x.x` 等内网地址 |
| .env 环境变量文件 | ✅ **通过** | 未创建 `.env` 文件，无环境变量泄露风险 |
| package.json | ✅ **通过** | 已设置 `"private": true`，无敏感配置 |
| README.md | ✅ **通过** | 仅包含项目介绍，无敏感信息 |
| 源代码文件 | ✅ **通过** | React、TypeScript、Rust 代码中无隐私泄露 |
| 文档文件 | ✅ **通过** | docs/ 目录文档仅含技术说明，无敏感信息 |
| `.gitignore` 配置 | ✅ **通过** | 已正确忽略 `node_modules/`、`dist/`、`release/`、`*.exe`、`*.zip` 等 |
| 隐藏文件（`.env*.local` 等） | ✅ **通过** | 未发现此类文件被跟踪 |

---

## 4. 修复建议汇总

| 优先级 | 问题 | 建议操作 | 涉及文件 |
|:------:|:-----|:---------|:---------|
| 🔴 **高** | 真实姓名 + 邮箱暴露 | 替换为占位符 `"Developer"` / `"dev@example.com"` | `scripts/rebuild-git-history.ps1`, `scripts/build-git-history.ps1` |
| 🟡 **中** | 绝对路径暴露 | 替换为 `Split-Path $PSScriptRoot` 动态获取路径 | `scripts/rebuild-git-history.ps1`, `scripts/build-git-history.ps1`, `scripts/启动开发环境.ps1` |
| 🟡 **中** | MSVC/SDK 版本硬编码 | 使用 `Get-ChildItem` 动态检测工具链版本 | `scripts/启动开发环境.ps1` |
| 🟢 **低** | GitHub 用户名关联 | 考虑使用组织账户或保持现状 | `.git/config` |
| 🟢 **低** | `.git-rewrite/` 安全确认 | 确认从未被提交到 Git 历史中 | `.git-rewrite/` 目录 |

---

## 5. 附录：AI 隐私检查提示词

> 以下提示词可在后续开发中提供给 AI，作为每次推送到 GitHub 前的自动检查依据。

````markdown
# AI 隐私安全检查提示词

在推送代码到 GitHub 公开仓库前，请执行以下检查：

## 必须检查的项目

1. **身份信息**
   - 搜索 `user.name` / `user.email`：确认没有真实姓名或邮箱硬编码
   - 搜索 `@xxx.com` / `@xxx.cn`：确认没有个人邮箱
   - 使用占位符替代：`"Developer"` / `"dev@example.com"`

2. **绝对路径**
   - 搜索 `[A-Z]:\\` 等 Windows 盘符路径
   - 搜索 `/home/`、`/Users/` 等 Linux/macOS 路径
   - 使用 `$PSScriptRoot`、`__dirname`、`process.cwd()` 等动态路径替代

3. **API 密钥与凭证**
   - 搜索 `api_key`、`apiKey`、`secret`、`token`、`password`、`auth`
   - 确认无硬编码的第三方服务凭证

4. **开发环境信息**
   - 确认没有硬编码的编译器版本、SDK 版本、工具链路径
   - 确认没有 `.env`、`*.local` 等环境配置文件被跟踪

5. **敏感文件**
   - 检查 `.gitignore` 是否覆盖了：`*.pem`、`*.key`、`*.crt`、`*.p12`
   - 检查是否误提交了 `release/`、`dist/`、`node_modules/` 等目录

6. **配置安全**
   - `package.json`：确认 `"private": true` 已设置
   - `.gitignore`：确认所有构建产物和敏感目录已被忽略

## 每次修改脚本/配置后快速复查

修改过以下文件后，务必复查上述检查项：
- `scripts/*.ps1`、`scripts/*.sh`
- `.gitignore`
- `package.json`
- 新增的 `.env*`、`*config*` 文件
````

---

*报告生成: 2026-05-16 | 审查工具: CodeBuddy AI Agent*
