# 项目精简清理报告

**项目名称**: Text Unifier  
**版本**: v3.2.2  
**清理日期**: 2026-05-15  
**报告目的**: 记录本次项目精简过程中所有代码/文件变更的详细说明，包括已执行的操作和待处理的建议。

---

## 目录

1. [总体统计](#1-总体统计)
2. [已删除的文件](#2-已删除的文件)
3. [已合并的文件](#3-已合并的文件)
4. [已修改的文件](#4-已修改的文件)
5. [未被清理但有精简潜力的项](#5-未被清理但有精简潜力的项)
6. [总体收益总结](#6-总体收益总结)

---

## 1. 总体统计

| 指标 | 操作前 | 操作后 | 变化 |
|---|---|---|---|
| `src/components` 文件数 | 21 | 16 | **-5 个文件** |
| `src/store/useStore.ts` 行数 | ~1057 | 889 | **-168 行 (-16%)** |
| `src/utils/ipc.ts` 行数 | ~82 | 60 | **-22 行 (-27%)** |
| `electron/main.ts` 行数 | ~136 | 129 | **-7 行** |
| `electron/preload.ts` 行数 | ~15 | 12 | **-3 行** |
| `electron/preload.d.ts` 行数 | ~17 | 15 | **-2 行** |
| `src/types/index.ts` 行数 | ~145 | 136 | **-9 行** |
| **总代码行数节省** | ~1452 | ~1241 | **-211 行 (-14.5%)** |

---

## 2. 已删除的文件

### 2.1 `src/components/FileDropZone.tsx`

| 属性 | 说明 |
|---|---|
| **操作** | 删除 |
| **行数** | ~190 行 |
| **操作原因** | 该组件是 V2.0 及之前的大面积文件拖拽上传区。V3.2 引入了新的拖拽交互机制（`DragOverlay` 全窗口拖拽遮罩 + `FileChipBar` 水平芯片式文件栏），替代了原 `FileDropZone`。经代码搜索确认，任何文件均未 import 此组件。 |
| **操作结果** | ✅ 文件已从磁盘删除，节省 190 行代码。 |

### 2.2 `src/components/FileSortList.tsx`

| 属性 | 说明 |
|---|---|
| **操作** | 删除 |
| **行数** | ~186 行 |
| **操作原因** | 该组件是 V2.0 的文件拖拽排序垂直列表。V3.2 将文件从垂直列表升级为水平横向流动芯片（`FileChipBar`），原 `FileSortList` 已完全废弃。经代码搜索确认，任何文件均未 import 此组件。 |
| **操作结果** | ✅ 文件已从磁盘删除，节省 186 行代码。 |

### 2.3 `src/components/SortableFileItem.tsx`

| 属性 | 说明 |
|---|---|
| **操作** | 删除 |
| **行数** | ~135 行 |
| **操作原因** | 该组件是 `FileSortList` 内部的单个可拖拽文件行，仅被 `FileSortList` 引用。随着 `FileSortList` 被删除，此组件也随之成为死代码。 |
| **操作结果** | ✅ 文件已从磁盘删除，节省 135 行代码。 |

### 2.4 `src/components/FormatButton.tsx`

| 属性 | 说明 |
|---|---|
| **操作** | 删除 |
| **行数** | ~102 行 |
| **操作原因** | 该组件是 V2.0 引入的「文档排版/还原」按钮，使用 Electron 后端 `formatDocument` IPC 进行文档排版。V3.1 引入了 `FormatPanel`（右侧面板排版增强）和 `applyProcessing`（纯前端 novelProcessor 处理流水线），完全替代了 `FormatButton` 的功能。经代码库搜索，`FormatButton` 在 `App.tsx` 和任何组件中均未被 import。 |
| **操作结果** | ✅ 文件已从磁盘删除，节省 102 行代码。 |

### 2.5 `src/components/UploadButton.tsx`

| 属性 | 说明 |
|---|---|
| **操作** | 删除（内容已合并到 `FileChipBar.tsx`） |
| **行数** | ~54 行 |
| **操作原因** | 该组件是一个仅包含按钮和隐藏 `<input type="file">` 的简单组件，仅被 `FileChipBar` 使用。为减少文件数量，将其代码直接内联到 `FileChipBar` 中。 |
| **操作结果** | ✅ 文件已从磁盘删除，内容已内联到 `FileChipBar.tsx`。 |

---

## 3. 已合并的文件

### 3.1 `UploadButton.tsx` → 内联到 `FileChipBar.tsx`

| 属性 | 说明 |
|---|---|
| **操作** | 内联合并 |
| **文件路径** | `src/components/FileChipBar.tsx` |
| **变更明细** | 将 `UploadButton` 的 JSX (`<button>` + `<input type="file">`)、`useRef`、`useState<isHover>` 状态、事件处理逻辑直接移入 `FileChipBar` 组件。移除了 `import { UploadButton }`。 |
| **操作结果** | `FileChipBar.tsx` 从 76 行增加到 111 行（+35 行），但 `UploadButton.tsx` 的 54 行已删除，**净节省 19 行**，同时减少了 1 个组件文件和 1 条 import 链。 |

---

## 4. 已修改的文件

### 4.1 `src/store/useStore.ts`

状态管理核心文件，原始 ~1057 行，修改后 **889 行**，**减少 168 行（16%）**。

#### 4.1.1 移除 `checkedHashes: Set<string>` 字段

| 属性 | 说明 |
|---|---|
| **操作** | 删除接口声明 + 初始值 + 所有引用 |
| **标记** | `@deprecated V2.0` |
| **操作原因** | 该字段是 V1.0 的段落勾选机制，V2.0 已被 `paragraphCheckedMap: Map<string, boolean>` 完全替代。存储中和 `setAnalysisResult`/`resetSession` 中对该字段的引用均为遗留代码。 |
| **操作结果** | 删除了接口声明、初始值（`new Set<string>()`）、`setAnalysisResult` 中的重置、`resetSession` 中的重置。 |

#### 4.1.2 移除 `toggleGroupCheck` action

| 属性 | 说明 |
|---|---|
| **操作** | 删除接口声明 + 实现 |
| **标记** | `@deprecated V2.0` |
| **操作原因** | 该 action 使用 `checkedHashes` 来切换组的勾选状态，已被 V2.0 的 `toggleGroupCheckV2`（使用三态联动 + `paragraphCheckedMap`）完全替代。经代码库搜索，没有任何组件调用此函数。 |
| **操作结果** | 删除了约 15 行代码。 |

#### 4.1.3 移除 `formatDocumentAction` action

| 属性 | 说明 |
|---|---|
| **操作** | 删除接口声明 + 完整实现 |
| **操作原因** | 该 action 是 V2.0「文档排版」的核心实现，调用后端 IPC `formatDocument` 进行排版。V3.1 引入了 `applyProcessing`，使用纯前端 `novelProcessor.ts` 完成清洗+排版+章节处理的完整流水线。`formatDocumentAction` 不再被任何组件调用（其唯一调用者 `FormatButton` 已被删除）。 |
| **操作结果** | 删除了约 85 行代码，包括后端 IPC 调用、文本拼接/拆分、段落重建、勾选状态迁移等逻辑。同时移除了文件顶部的 `import { formatDocument } from '../utils/ipc'`。 |

#### 4.1.4 移除 `_updateChapterList` action

| 属性 | 说明 |
|---|---|
| **操作** | 删除接口声明 + 实现 |
| **操作原因** | 该函数的作用是从当前段落文本中重新提取章节列表。其逻辑与 `applyProcessing` 方法末尾的章节检测代码（相同正则 + 相同遍历）完全重复。且没有任何组件显式调用此函数。 |
| **操作结果** | 删除了约 25 行重复代码。 |

#### 4.1.5 移除 `computeActivePreview` 导出函数

| 属性 | 说明 |
|---|---|
| **操作** | 删除导出函数 |
| **操作原因** | 该函数使用已废弃的 `checkedHashes` 参数过滤预览段落。V2.0 后组件直接使用 `previewParagraphs` 结合 `paragraphCheckedMap` 进行过滤，不再需要此函数。任何组件均未导入此函数。 |
| **操作结果** | 删除了约 15 行代码。 |

#### 4.1.6 移除 `serializeCheckedHashes` / `deserializeCheckedHashes` 导出函数

| 属性 | 说明 |
|---|---|
| **操作** | 删除导出函数 |
| **操作原因** | 这两个辅助函数用于 `checkedHashes` 的 JSON 序列化/反序列化。随着 `checkedHashes` 被删除，这两个辅助函数也随之成为死代码。 |
| **操作结果** | 删除了约 12 行代码。 |

---

### 4.2 `src/utils/ipc.ts`

前端 IPC 通信封装，原始 ~82 行，修改后 **60 行**。

#### 4.2.1 移除 `scanPreprocessedTexts` 函数

| 属性 | 说明 |
|---|---|
| **操作** | 删除导出函数 |
| **操作原因** | 该函数是 V3.1 新增的预处理文本扫描接口，允许前端先清洗文本再传给后端归一化去重。然而在实际迭代中，清洗逻辑全部在 `novelProcessor.ts`（纯前端）中完成，且 `applyProcessing` 使用的是 `scanFiles`（直接传文件路径），从未调用此函数。 |
| **操作结果** | 删除了约 10 行代码。 |

#### 4.2.2 移除 `formatDocument` 函数

| 属性 | 说明 |
|---|---|
| **操作** | 删除导出函数 |
| **操作原因** | 该函数是 V2.0 文档排版的后端 IPC 调用。V3.1 后前端排版全部由 `applyProcessing` → `novelProcessor.ts` 完成，此函数仅被已删除的 `formatDocumentAction` 调用。后端 IPC handler (`electron/main.ts` 中的 `format-document`) 保留以兼容原生 Rust 模块的 API。 |
| **操作结果** | 删除了约 6 行代码。 |

#### 4.2.3 移除 `@deprecated` 注解

| 属性 | 说明 |
|---|---|
| **操作** | 删除 JSDoc 注解 |
| **操作原因** | `scanFiles` 函数上标记的 `@deprecated V3.1 推荐使用 scanPreprocessedTexts` 已过时。`scanPreprocessedTexts` 已被移除，`scanFiles` 是当前唯一活跃的文件扫描接口。 |
| **操作结果** | 清理了过时的注释。 |

#### 4.2.4 移除 `import { FormatResult }` 类型导入

| 属性 | 说明 |
|---|---|
| **操作** | 删除 import |
| **操作原因** | `FormatResult` 类型仅被已删除的 `formatDocument` 函数引用。且该类型已从 `src/types/index.ts` 中移除。 |
| **操作结果** | import 行清理。 |

---

### 4.3 `electron/main.ts`

Electron 主进程，原始 ~136 行，修改后 **129 行**。

#### 4.3.1 移除 `scan-preprocessed-texts` IPC handler

| 属性 | 说明 |
|---|---|
| **操作** | 删除 IPC handler |
| **操作原因** | 对应的前端调用 `scanPreprocessedTexts` 已被删除，此 IPC handler 在前端无任何调用方。handler 内部的 `nativeModule.scanPreprocessedTexts` 后端函数仍存在于 Rust 原生模块中，但无调用入口。 |
| **操作结果** | 删除了约 7 行代码。 |

---

### 4.4 `electron/preload.ts`

Electron 预加载脚本，原始 ~15 行，修改后 **12 行**。

#### 4.4.1 移除 `scanPreprocessedTexts` 暴露

| 属性 | 说明 |
|---|---|
| **操作** | 删除 contextBridge 暴露 |
| **操作原因** | `scanPreprocessedTexts` 在前端无任何调用（`ipc.ts` 中对应的包装函数已被删除），无需暴露到 `window.electronAPI`。 |
| **操作结果** | 删除了约 3 行代码。 |

---

### 4.5 `electron/preload.d.ts`

Electron API 类型声明，原始 ~17 行，修改后 **15 行**。

#### 4.5.1 移除 `scanPreprocessedTexts` 类型声明

| 属性 | 说明 |
|---|---|
| **操作** | 删除接口方法声明 |
| **操作原因** | 与方法暴露的删除保持一致。 |
| **操作结果** | 删除了 1 行。 |

#### 4.5.2 将 `FormatResult` 类型导入替换为内联类型

| 属性 | 说明 |
|---|---|
| **操作** | 替换类型引用 |
| **操作原因** | `FormatResult` 已从 `src/types/index.ts` 中删除（因无任何前端组件使用该类型）。但 `preload.d.ts` 中 `formatDocument` 方法仍需要返回类型描述，故将 `import { FormatResult }` 替换为内联的类型字面量 `{ formattedText: string; paragraphCount: number; protectedBlocks: number; mergedBlocks: number }`。 |
| **操作结果** | 修改了 import 和方法类型签名。 |

---

### 4.6 `src/types/index.ts`

类型定义文件，原始 ~145 行，修改后 **136 行**。

#### 4.6.1 移除 `FormatResult` 接口

| 属性 | 说明 |
|---|---|
| **操作** | 删除接口定义 |
| **操作原因** | `FormatResult` 是 V2.0 后端排版返回结果的数据结构，仅被已删除的 `formatDocument` 前端函数使用。无任何活跃组件/文件引用此类型。 |
| **操作结果** | 删除了约 9 行代码。 |

---

### 4.7 `src/components/FileChipBar.tsx`

文件标签栏组件，原始 ~76 行，修改后 **111 行**。

#### 4.7.1 内联 `UploadButton` 组件

| 属性 | 说明 |
|---|---|
| **操作** | 代码内联 |
| **变更** | 增加了 `fileInputRef`（useRef）、`isHover`/`setIsHover`（useState）状态、`<button>` + `<input type="file">` JSX。移除了 `import { UploadButton }`。 |
| **操作原因** | 减少组件文件数量，缩短 import 链。 |
| **操作结果** | FileChipBar +35 行，UploadButton -54 行，净 -19 行。 |

---

## 5. 未被清理但有精简潜力的项

以下项目经评估后未被本次操作覆盖，但仍有精简空间，供参考：

### 5.1 `src-tauri/` 废弃目录

| 属性 | 说明 |
|---|---|
| **路径** | `./src-tauri/` |
| **当前状态** | 已完全被 `.gitignore` 忽略 |
| **文件数量** | ~12592 个文件（其中 ~12524 个为 Rust `target/` 编译产物） |
| **未清理原因** | 虽然不会进入 Git 仓库，但本地磁盘占用大量空间。需要确认未来是否还有回退到 Tauri 的需求。 |
| **建议操作** | 如确认不再需要 Tauri 版本，可安全删除整个 `src-tauri/` 目录，释放约数百 MB 磁盘空间。 |

### 5.2 `docs/` 目录旧版本文档

| 属性 | 说明 |
|---|---|
| **路径** | `./docs/` |
| **当前状态** | 包含 34 个 Markdown 文档，涵盖 V3.0 ~ V3.2.2 的 PRD、架构设计、接口规范、代码变更记录、自审报告等 |
| **未清理原因** | 文档具有历史存档价值。部分旧版本文档（如 V3.0 接口规范、V3.0 数据库设计）当前版本已不再适用，但删除可能影响开发历史追溯。 |
| **建议操作** | 将 V3.0/V3.1 系列的旧文档移至 `archive/` 目录归档，`docs/` 仅保留当前 V3.2.2 相关文档。保留以下即可：`Bug修复说明_V3.2.2.md`、`代码变更记录_V3.2.2.md`、`修复方案_BUG-031_多文件滚动.md`、`功能集成分析_NovelProcessor.md`、`自审代码报告_V3.2.2.md`。 |

### 5.3 `electron/preload.ts` 仍暴露 `formatDocument`

| 属性 | 说明 |
|---|---|
| **当前状态** | `preload.ts` 仍通过 `contextBridge` 暴露 `formatDocument` 方法，对应的 IPC handler `format-document` 仍在 `electron/main.ts` 中注册。 |
| **未清理原因** | 虽然前端无组件调用此 IPC，但后端的 Rust `nativeModule.formatDocument` 函数仍存在，保留 handler 可保持接口的向后兼容，或在未来功能扩展时恢复。 |
| **建议操作** | 如确定不再需要后端排版功能，可删除 preload 暴露 + IPC handler。 |

### 5.4 `src/utils/ipc.ts` 中的重试逻辑

| 属性 | 说明 |
|---|---|
| **当前状态** | `withRetry` 函数实现了指数退避重试（最多 3 次），每次 IPC 调用都经过此包装。 |
| **未清理原因** | Electron IPC 是本地进程间通信，理论上不会出现网络超时。重试逻辑增加了代码复杂度但极少真正触发。 |
| **建议操作** | 如果 Electron IPC 稳定性足够，可考虑移除重试逻辑，简化代码结构。 |

### 5.5 `archive/` 目录整理

| 属性 | 说明 |
|---|---|
| **路径** | `./archive/` |
| **当前状态** | 包含 v1.0~v2.0.2 的历史发布包和文档 |
| **未清理原因** | 这些是历史归档，保留可追溯需求变更和架构演进。 |
| **建议操作** | 如磁盘空间紧张，可将旧版本打包为 `.zip` 后移除散文件。 |

---

## 6. 总体收益总结

### 代码行数节省

| 维度 | 节省量 |
|---|---|
| 组件文件数 | **21 → 16**（-5 个文件） |
| 源代码行数 | **~1452 → ~1241 行**（-211 行，-14.5%） |
| store 精简 | -168 行（最大单项收益） |
| IPC 层精简 | -22 行（去掉 2 个未使用函数） |

### 代码质量提升

- ✅ 消除了所有 `@deprecated` 标记的代码
- ✅ 删除了重复逻辑（`_updateChapterList` vs `applyProcessing` 中的章节检测）
- ✅ 消除了 V1.0 → V2.0 过渡期遗留的兼容代码
- ✅ 移除了前端从未调用的 IPC 接口
- ✅ 减少了 import 链深度（UploadButton 内联）

---

*报告生成: 2026-05-15 | 清理工具: CodeBuddy AI Agent*
