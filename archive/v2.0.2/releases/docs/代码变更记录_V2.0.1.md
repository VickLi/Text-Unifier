# 代码变更记录 V2.0.1

| 项目 | 内容 |
| :--- | :--- |
| **项目名称** | 文档终版确定器（Text Unifier） |
| **版本号** | V2.0.1 |
| **基线版本** | V2.0.0 |
| **变更类型** | Bug 修复 |
| **发布日期** | 2026-05-09 |

> 本记录用于版本追溯，列出 V2.0.0 → V2.0.1 的所有代码变更。

---

## 变更总览

| 指标 | 数值 |
| :--- | :---: |
| **修复 Bug 数** | 5 |
| **修改文件数** | 4 |
| **新增测试用例** | 2 |
| **删除冗余代码行** | 3 |
| **新增代码行** | ~40 |

---

## 文件级变更

### 1. `src/store/useStore.ts` — 修复 BUG-V2.0-001

**变更位置**: `removeFile` action

**变更前**（~5 行）:
```typescript
removeFile: (path) =>
    set((state) => ({
      fileList: state.fileList.filter((f) => f.path !== path),
      sortedFileList: state.sortedFileList.filter((f) => f.path !== path),
    })),
```

**变更后**（~18 行）:
```typescript
removeFile: (path) =>
    set((state) => {
      const newFileList = state.fileList.filter((f) => f.path !== path);
      const newSortedList = state.sortedFileList.filter((f) => f.path !== path);
      const shouldReset = newFileList.length === 0;
      return {
        fileList: newFileList,
        sortedFileList: newSortedList,
        duplicateGroups: shouldReset ? [] : state.duplicateGroups,
        originalPreview: shouldReset ? [] : state.originalPreview,
        previewParagraphs: shouldReset ? [] : state.previewParagraphs,
        paragraphCheckedMap: shouldReset ? new Map() : state.paragraphCheckedMap,
        status: shouldReset ? 'idle' : state.status,
        formatSnapshot: shouldReset ? null : state.formatSnapshot,
        canRevert: shouldReset ? false : state.canRevert,
      };
    }),
```

**变更类型**: 功能修复 + 防御性编程

---

### 2. `src/components/PreviewParagraph.tsx` — 修复 BUG-V2.0-002

**变更位置**: `handleCheckboxChange` 回调

**变更前**:
```typescript
onCheckToggle(paragraph.id, (e.nativeEvent as MouseEvent).shiftKey);
```

**变更后**:
```typescript
const nativeEvent = e.nativeEvent;
const isShiftKey = nativeEvent instanceof MouseEvent
  ? nativeEvent.shiftKey
  : false;
onCheckToggle(paragraph.id, isShiftKey);
```

**变更类型**: 类型安全修复

---

### 3. `src-tauri/src/document_formatter.rs` — 修复 BUG-V2.0-003 + BUG-V2.0-004

#### BUG-V2.0-003: 删除 `no_period_end_re`

| 行号 | 变更 | 说明 |
| :--- | :--- | :--- |
| 字段定义 | 删除 `no_period_end_re: Regex` | 冗余字段（与 `sentence_end_re` 重复） |
| `new()` 构造 | 删除 `no_period_end_re: Regex::new(...)` | 对应字段初始化 |
| `is_protected_block()` | `self.no_period_end_re` → `self.sentence_end_re` | 统一使用已存在的句尾标点正则 |
| 变量名 | `no_period_lines` → `lines_without_period` | 语义更清晰 |

#### BUG-V2.0-004: 新增测试用例（在 `#[cfg(test)]` 模块末尾）

```rust
#[test]
fn test_tab_character_handling()       // Tab 字符保持原样
#[test]
fn test_mixed_tab_and_space()           // Tab + 空格混合场景
```

---

### 4. `src-tauri/src/lib.rs` — 修复 BUG-V2.0-005

**变更位置**: `export_text` 函数入口

**新增代码**（在函数开头，保存对话框之前）:
```rust
if paragraphs.is_empty() || paragraphs.iter().all(|p| p.trim().is_empty()) {
    return Err("导出失败: 没有可导出的内容（所有段落已被排除）".to_string());
}
```

---

## 测试变更

| 测试文件 | 变更 | 原测试数 | 现测试数 |
| :--- | :--- | :---: | :---: |
| `document_formatter.rs` | +2 测试用例 | 10 | **12** |
| `text_normalizer.rs` | 不变 | 5 | 5 |
| `paragraph_index.rs` | 不变 | 4 | 4 |
| `file_processor.rs` | 不变 | 2 | 2 |
| **合计** | **+2** | **23** | **25** |

---

## 依赖变更

**无新增/删除依赖。**

---

## 构建产物

| 产物 | 路径 | 大小 |
| :--- | :--- | :--- |
| Release 二进制 | `releases/v2.0/text-unifier-v2.0.exe` | ~6.1MB |
| 完整源码包 | `releases/v2.0/text-unifier-src-v2.0.zip` | ~46KB |

---

*记录生成日期：2026-05-09*
*版本追溯基线：V2.0.0*
