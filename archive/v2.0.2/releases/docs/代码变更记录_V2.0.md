# 代码变更记录 V2.0

| 项目 | 内容 |
| :--- | :--- |
| **项目名称** | 文档终版确定器（Text Unifier） |
| **版本号** | V2.0 |
| **基线版本** | V1.0.1 |
| **基线 Tag** | `v1.0.1` |
| **发布日期** | 2026-05-09 |

> 本记录用于版本追溯，列出 V1.0.1 → V2.0 的所有代码变更。

---

## 一、新增文件

### 1.1 Rust 后端

| 文件路径 | 行数 | 说明 |
| :--- | :---: | :--- |
| `src-tauri/src/document_formatter.rs` | ~290 | **RQ-03 核心引擎**：文档排版去硬回车实现 |
| | | · `DocumentFormatter` 结构体 + 5 个 regex |
| | | · `format()` 主流程：分割 → 细分 → 保护检测 → 合并 → 后处理 |
| | | · `is_protected_block()` 列表/诗歌检测 |
| | | · `merge_lines()` 行合并 |
| | | · `refine_split_safe()` 混合策略细粒度分段 |
| | | · 10 个单元测试覆盖全部边界场景 |

### 1.2 前端 React 组件

| 文件路径 | 行数 | 说明 |
| :--- | :---: | :--- |
| `src/components/TriStateCheckbox.tsx` | ~55 | **三态复选框**：checked/unchecked/indeterminate |
| `src/components/SortableFileItem.tsx` | ~120 | **可拖拽文件行**：拖拽手柄、编码标签、主文件徽章 |
| `src/components/FileSortList.tsx` | ~155 | **文件排序列表**：@dnd-kit 拖拽上下文 |
| `src/components/FormatButton.tsx` | ~110 | **排版/还原按钮**：含已排除计数显示 |
| `src/hooks/useShiftSelect.ts` | ~55 | **Shift 多选 Hook**：段落范围选择逻辑 |

---

## 二、变更文件

### 2.1 Rust 后端

| 文件路径 | 变更类型 | 变更内容 |
| :--- | :--- | :--- |
| `src-tauri/src/lib.rs` | +25 / -1 | · 新增 `mod document_formatter` |
| | | · 新增 `format_document` Tauri Command |
| | | · 注册 `format_document` 到 invoke_handler |

### 2.2 前端 TypeScript

| 文件路径 | 变更类型 | 变更内容 |
| :--- | :--- | :--- |
| `src/types/index.ts` | +20 / -5 | · 新增 `SortableFile`、`FormatResult`、`TriState`、`SelectAllState`、`FileMeta` |
| | | · `AnalysisReport` 新增 `files_metadata` 字段 |
| `src/utils/ipc.ts` | +12 / -1 | · 新增 `formatDocument()` IPC 调用函数 |

### 2.3 前端 Store

| 文件路径 | 变更类型 | 变更内容 |
| :--- | :--- | :--- |
| `src/store/useStore.ts` | 重写 (~350 行) | · **V1.0 保留**：核心 Actions 保持兼容 |
| | | · **RQ-01 新增**：`sortedFileList`、`activeDragFileId`、`reorderFiles()`、`syncFileListToSorted()` |
| | | · **RQ-02 新增**：`previewParagraphs`、`paragraphCheckedMap`、`lastClickedParagraphId`、`toggleParagraphCheck()`、`batchToggleParagraphCheck()`、`toggleGroupCheckV2()`、`selectAllParagraphs()`、`deselectAllParagraphs()` |
| | | · **RQ-03 新增**：`formatSnapshot`、`isFormatting`、`canRevert`、`formatDocumentAction()`、`revertFormatting()` |
| | | · **派生函数**：`computeExcludedCount()`、`computeSelectAllState()`、`computeGroupState()`、`computeExportParagraphs()` |

### 2.4 前端组件

| 文件路径 | 变更类型 | 变更内容 |
| :--- | :--- | :--- |
| `src/App.tsx` | 重写 (~175 行) | · 集成 `FileSortList`、`FormatButton` |
| | | · 版本号更新为 v2.0 |
| | | · 文件选择逻辑适配 sortedFileList |
| | | · 添加自定义事件监听 `sortable-files-selected` |
| `src/components/DuplicateList.tsx` | 重写 | · 使用 `computeGroupState` 三态联动 |
| | | · 使用 `toggleGroupCheckV2` 替代 `toggleGroupCheck` |
| `src/components/DuplicateItem.tsx` | 重写 | · 使用 `TriStateCheckbox` 替代普通 Checkbox |
| | | · 新增 `indeterminate` 状态显示 |
| `src/components/PreviewParagraph.tsx` | 重写 | · 新增 Checkbox 和勾选回调 |
| | | · 取消勾选时段落 opacity-30 grayscale |
| | | · 新增「已排除」徽章 |
| `src/components/PreviewPanel.tsx` | 重写 | · 集成 `useShiftSelect` Hook |
| | | · 新增全选/取消全选按钮 |
| | | · 全部排除时空状态提示 |
| | | · 使用 `paragraphCheckedMap` 替代 `checkedHashes` |
| `src/components/ExportButton.tsx` | 重写 | · 使用 `computeExportParagraphs` 计算导出段落 |
| | | · 使用 `paragraphCheckedMap` 快照确保导出一致性 |

### 2.5 配置文件

| 文件路径 | 变更类型 | 变更内容 |
| :--- | :--- | :--- |
| `package.json` | +3 | · 新增 `@dnd-kit/core`、`@dnd-kit/sortable`、`@dnd-kit/utilities` 依赖 |

---

## 三、删除/废弃

| 文件/功能 | 说明 |
| :--- | :--- |
| `checkedHashes` (部分废弃) | V2.0 主逻辑使用 `paragraphCheckedMap`，`checkedHashes` 保留兼容 |
| `toggleGroupCheck` (标记废弃) | V2.0 使用 `toggleGroupCheckV2` 替代（三态联动） |

---

## 四、API 变更

### 新增 Tauri Command

```rust
#[tauri::command]
fn format_document(text: String) -> Result<FormatResult, String>
```

### 新增 IPC 函数

```typescript
export async function formatDocument(text: string): Promise<FormatResult>
```

---

## 五、依赖变更

### 新增依赖

| 包名 | 版本 | 用途 |
| :--- | :---: | :--- |
| `@dnd-kit/core` | ^6.1.0 | 拖拽排序核心库 |
| `@dnd-kit/sortable` | ^8.0.0 | 可排序列表 |
| `@dnd-kit/utilities` | ^3.2.2 | CSS transform 工具 |

### 无移除依赖

---

## 六、构建产物

| 产物 | 路径 | 大小 |
| :--- | :--- | :--- |
| Rust release 二进制 | `src-tauri/target/release/text-unifier.exe` | ~8MB |
| 前端 dist | `dist/` | ~225KB (JS+CSS) |
| 完整源码包 | `releases/v2.0/text-unifier-v2.0.zip` | - |

---

*记录生成日期：2026-05-09*
*版本追溯基线：V1.0.1*
