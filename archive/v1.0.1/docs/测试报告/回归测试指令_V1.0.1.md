# Text Unifier V1.0.1 — 回归测试指令（修复验证版）

> **发布日期**: 2026-05-06
> **修复版本**: v1.0.1（修复 23 个 Bug）
> **测试策略**: 定向验证 + 全链路冒烟 + 判定矩阵

---

## 一、修复模块定向验证

### 阶段 1: 🔴 Critical Bug 修复验证（3项）

---

#### RT-001: BUG-001 中文切片修复

| 项目 | 内容 |
| :--- | :--- |
| **修复文件** | `src-tauri/src/paragraph_index.rs` |
| **修复函数** | `make_snippet()` |
| **原始问题** | 字节索引 `[..30]` 导致中文 panic |
| **修复方式** | 改用 `content.chars().take(10)` 字符索引 |

**验证步骤**：
```powershell
# 1. 创建含中文字符的重复文件
@"
项目启动会议纪要
与会人员：张三、李四、王五
"@ | Out-File -Encoding utf8 test_cn_A.txt

@"
项目启动会议纪要
额外的独有内容
"@ | Out-File -Encoding utf8 test_cn_B.txt

# 2. 启动应用，导入两个文件
Start-Process ".\deploy\TextUnifier_v1.0.1.exe"

# 3. 确认应用不崩溃
# 4. 确认左侧重复组显示 "项目启动会议纪要" 的 snippet 完整
```

**通过标准**: ✅ 应用不崩溃，中文 snippet 显示完整字符（如"项目启动会议纪要..."）

---

#### RT-002: BUG-002 编码降级修复

| 项目 | 内容 |
| :--- | :--- |
| **修复文件** | `src-tauri/src/file_processor.rs` |
| **修复函数** | `read_file_content()` |
| **原始问题** | 仅支持 WINDOWS_1252 降级，GBK 乱码 |
| **修复方式** | UTF-8 → GB18030 → WINDOWS-1252 → SHIFT-JIS |

**验证步骤**：
```powershell
# 1. 创建 GBK 编码文件（使用 PowerShell）
$content = "项目启动会议纪要`r`n待办事项：`r`n1. 确认预算"
[System.IO.File]::WriteAllBytes("test_gbk.txt", [System.Text.Encoding]::GetEncoding(936).GetBytes($content))

# 2. 创建相同内容的 UTF-8 文件
$content | Out-File -Encoding utf8 test_utf8.txt

# 3. 启动应用，同时导入两个文件
# 4. 确认 "项目启动会议纪要" 和 "待办事项：" 等被识别为重复
```

**通过标准**: ✅ GBK 文件内容正确显示，与 UTF-8 相同段落可匹配

---

#### RT-003: BUG-003 预览顺序修复

| 项目 | 内容 |
| :--- | :--- |
| **修复文件** | `src-tauri/src/paragraph_index.rs` |
| **修复方式** | HashMap → IndexMap（保持插入顺序） |
| **原始问题** | 多次分析预览顺序不同 |

**验证步骤**：
```powershell
# 1. 创建有序文件
"A段落`nB段落`nC段落" | Out-File -Encoding utf8 test_order_A.txt
"D段落`nA段落`nE段落" | Out-File -Encoding utf8 test_order_B.txt

# 2. 导入分析，记录预览顺序
# 3. 点击"重新开始"
# 4. 再次导入同样的文件
# 5. 对比两次预览段落顺序
```

**通过标准**: ✅ 多次导入同一组文件，预览段落顺序完全一致（A, B, C, D, E）

---

### 阶段 2: 🟠 Major Bug 修复验证（7项）

#### RT-004: BUG-004 哈希碰撞处理

| 验证内容 | 方法 |
| :--- | :--- |
| 碰撞处理 | 审查 `paragraph_index.rs` 中的 while 循环碰撞检测逻辑 |
| **通过标准** | 碰撞代码使用 `loop { unique_hash = format!(...); if !contains_key { break; } }` |

#### RT-005: BUG-005 线程安全

| 验证内容 | 方法 |
| :--- | :--- |
| Regex 线程安全 | 审查 `lib.rs` 中 `TextNormalizer` 创建位置 |
| **通过标准** | `TextNormalizer::new()` 在 `par_iter` 闭包内，而非外部 |

#### RT-006: BUG-006 文件元数据

| 验证内容 | 方法 |
| :--- | :--- |
| 元数据暴露 | 审查 `AnalysisReport` 是否包含 `files_metadata` |
| **通过标准** | `duplicate_resolver.rs` 中 `FileMeta` 结构体包含 `file_name, file_size, modified` |

#### RT-007: BUG-007 Set 序列化

| 验证内容 | 方法 |
| :--- | :--- |
| 序列化支持 | 审查 `useStore.ts` 中的序列化/反序列化辅助函数 |
| **通过标准** | 存在 `serializeCheckedHashes` / `deserializeCheckedHashes` 函数 |

#### RT-008: BUG-008 超时控制

| 验证内容 | 方法 |
| :--- | :--- |
| 超时机制 | 审查 `App.tsx` 中 `Promise.race` 实现 |
| **通过标准** | `SCAN_TIMEOUT_MS = 60000`，有 timeoutPromise |

#### RT-009: BUG-009 导出竞态

| 验证内容 | 方法 |
| :--- | :--- |
| 状态快照 | 审查 `ExportButton.tsx` 导出逻辑 |
| **通过标准** | 导出开始时 `const snapshotHashes = new Set(checkedHashes)` |

#### RT-010: BUG-010 文件验证

| 验证内容 | 方法 |
| :--- | :--- |
| 前端验证 | 审查 `FileDropZone.tsx` |
| **通过标准** | 扩展名检查 + 100MB 大小限制 + MIME 类型检查 |

---

### 阶段 3: 🔵 新发现 Bug 验证

#### RT-011: BUG-024 Tab 字符处理

| 项目 | 内容 |
| :--- | :--- |
| **文件** | `src-tauri/src/text_normalizer.rs:34-36` |
| **问题** | `is_control()` 过滤了 `\t`，违背 PRD |
| **影响** | Tab 分隔单词被合并 |
| **修复建议** | 在 filter 中添加 `c == '\t'` 排除 |
| **通过标准** | ⚠️ 本次不要求修复，仅记录 |

---

### 阶段 4: 🟢 全链路冒烟测试

#### SM-01: 基本功能冒烟

| 步骤 | 操作 | 预期结果 |
| :--- | :--- | :--- |
| 1 | 启动应用 | 窗口标题"文档终版确定器 - Text Unifier" |
| 2 | 拖入 2 个含重复的 .txt 文件 | 加载动画后显示重复组 |
| 3 | 勾选重复组 | 预览实时移除段落 |
| 4 | 取消勾选 | 段落恢复显示 |
| 5 | 悬停段落 | 显示来源浮层 |
| 6 | 点击导出 | 系统保存对话框 |
| 7 | 保存文件 | 文件内容与预览一致 |

#### SM-02: 中文 + 多文件冒烟

| 步骤 | 操作 | 预期结果 |
| :--- | :--- | :--- |
| 1 | 同时拖入 3 个中文 .txt 文件 | 不崩溃，正常分析 |
| 2 | 检查 snippet | 中文不截断乱码 |
| 3 | 勾选→勾选→取消 | 实时刷新 |

#### SM-03: 边界场景冒烟

| 步骤 | 操作 | 预期结果 |
| :--- | :--- | :--- |
| 1 | 导入空文件 + 正常文件 | 不崩溃 |
| 2 | 导入 GBK 编码文件 | 正确显示中文 |
| 3 | 快速点击"重新开始" | 状态一致 |
| 4 | 缩放到 900x600 最小尺寸 | 布局完整 |

---

## 二、判定矩阵

根据回归测试结果，判断**是否继续修复 Bug**（即是否进入下一轮）：

```text
                           ┌─────────────────────────────┐
                           │     回归测试结果判定          │
                           │                             │
                           │ Phase 1 Critical: 全部通过?   │
                           │ Phase 2 Major:    全部通过?   │
                           │ Phase 4 冒烟:     全部通过?   │
                           └──────────┬──────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
                    ▼                 ▼                 ▼
              ✅ 全部通过          ⚠️ 部分失败         ❌ 关键失败
              ──────────          ──────────          ──────────
          建议发布 V1.0.1      修复失败项后重测      架构重新评估
          进入 V1.1 规划       (限 2 轮迭代)         (返回设计阶段)
```

### 停止修复条件

| 条件 | 操作 |
| :--- | :--- |
| 3 轮迭代后仍有 Critical Bug | 🛑 暂停，架构评审 |
| 5 轮迭代后仍有 Major Bug | 🛑 暂停，方案评审 |
| 修复引入新 Bug 比例 > 30% | 🛑 暂停，修复方案评审 |

---

## 三、执行检查表

```markdown
## 回归测试执行记录 — v1.0.1

### 测试日期: _______________
### 测试人员: _______________

### Phase 1: Critical 修复验证
- [ ] RT-001 中文切片 (BUG-001) — 含中文文件导入不崩溃
- [ ] RT-002 编码降级 (BUG-002) — GBK 文件正确显示
- [ ] RT-003 预览顺序 (BUG-003) — 多次分析顺序一致

### Phase 2: Major 修复验证
- [ ] RT-004 哈希碰撞 (BUG-004) — while 循环碰撞检测
- [ ] RT-005 线程安全 (BUG-005) — Normalizer 在线程闭包内
- [ ] RT-006 文件元数据 (BUG-006) — AnalysisReport.files_metadata
- [ ] RT-007 Set 序列化 (BUG-007) — serialize/deserialize 函数
- [ ] RT-008 超时控制 (BUG-008) — 60 秒超时
- [ ] RT-009 导出竞态 (BUG-009) — 状态快照
- [ ] RT-010 文件验证 (BUG-010) — 大小+MIME检查

### Phase 3: 新发现 Bug
- [ ] RT-011 Tab 字符 (BUG-024) — 记录待后续修复

### Phase 4: 全链路冒烟
- [ ] SM-01 基本功能冒烟
- [ ] SM-02 中文多文件冒烟
- [ ] SM-03 边界场景冒烟

### 判定结果
- [ ] ✅ 全部通过 → 建议发布 V1.0.1
- [ ] ⚠️ 部分失败 → 第 ___ 轮修复（限 2 轮）
- [ ] ❌ 关键失败 → 架构评审
```

---

## 四、构建与部署

### 构建命令

```bash
# Rust 后端构建
cd src-tauri
cargo build --release

# 前端构建
cd ..
npm run build

# 部署包打包
$deployDir = "deploy"
New-Item -ItemType Directory -Path $deployDir -Force | Out-Null
Copy-Item "src-tauri\target\release\text-unifier.exe" "$deployDir\TextUnifier_v1.0.1.exe"
Compress-Archive -Path "$deployDir\*" -DestinationPath "$deployDir\TextUnifier_v1.0.1_fixed.zip" -Force
```

---

*指令由 AI 测试验证系统自动生成*
