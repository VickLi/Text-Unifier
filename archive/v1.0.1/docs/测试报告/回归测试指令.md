# Text Unifier V1.0 - 回归测试指令

> **发布日期**: 2026-05-06
> **目标**: 验证 Critical + Major Bug 修复效果，确认无回归
> **测试策略**: 定向验证修复模块 + 全链路冒烟测试

---

## 一、修复任务清单与验证指令

### 阶段 1: Critical Bug 修复 (必须修复)

---

#### 🔴 RT-01: 验证 BUG-001 修复（字符串切片 Panic）

**修复文件**: `src-tauri/src/paragraph_index.rs`
**修改位置**: 第 110-114 行，`snippet` 字段生成逻辑
**修复要求**: 将字节索引 `[..30]` 改为字符索引 `.chars().take(10)`

**验证步骤**:
```bash
# 1. 创建测试文件
echo "这是一段测试文本用于验证重复检测功能是否正常工作" > test_1.txt
echo "这是一段测试文本用于验证重复检测功能是否正常工作" > test_2.txt
echo "这是另一个段落，测试多段落场景" >> test_1.txt
echo "这是另一个段落，测试多段落场景" >> test_2.txt

# 2. 构建并运行应用
cd src-tauri && cargo build --release

# 3. 导入 test_1.txt 和 test_2.txt
# 4. 确认应用不崩溃，左侧显示重复组
# 5. 确认 snippet 正确显示中文摘要（不截断字符）
```

**通过标准**: ✅ 含中文的重复段落不触发 panic，snippet 显示完整字符

---

#### 🔴 RT-02: 验证 BUG-002 修复（编码降级策略）

**修复文件**: `src-tauri/src/file_processor.rs`
**修改位置**: 第 20-24 行，降级编码逻辑
**修复要求**: 添加 GB18030 编码探测链

**验证步骤**:
```powershell
# 1. 使用 PowerShell 创建 GBK 编码文件
$gbkContent = "项目启动会议纪要`r`n待办事项清单"
[System.IO.File]::WriteAllBytes("gbk_test.txt", [System.Text.Encoding]::GetEncoding(936).GetBytes($gbkContent))

# 2. 创建 UTF-8 编码的相同内容文件
$utf8Content = "项目启动会议纪要`r`n待办事项清单"
[System.IO.File]::WriteAllBytes("utf8_test.txt", [System.Text.Encoding]::UTF8.GetBytes($utf8Content))

# 3. 导入两个文件
# 4. 确认 "项目启动会议纪要" 被识别为重复
```

**通过标准**: ✅ GBK 编码文件内容正确显示，与 UTF-8 文件的相同段落可匹配为重复

---

#### 🔴 RT-03: 验证 BUG-003 修复（预览段落顺序）

**修复文件**: `src-tauri/src/paragraph_index.rs`
**修改位置**: 第 96-100 行，`analyze()` 方法
**修复要求**: 使用 `IndexMap` 或维护插入顺序列表

**验证步骤**:
```bash
# 1. 创建一个包含有序段落的测试文件集
#    file_A.txt: A段，B段，C段
#    file_B.txt: D段，A段，E段
# 2. 导入并分析
# 3. 记录预览段落顺序: A, B, C, D, E
# 4. 点击"重新开始"
# 5. 再次导入相同文件
# 6. 确认预览顺序与上次一致
```

**通过标准**: ✅ 多次导入同一组文件，预览段落顺序完全一致

---

### 阶段 2: Major Bug 修复 (建议修复)

---

#### 🟠 RT-04: 验证 BUG-004 修复（哈希碰撞处理）

**修复文件**: `src-tauri/src/paragraph_index.rs`
**修改位置**: 第 62-79 行
**修复要求**: while 循环而非 if 处理碰撞

**验证方法**: 单元测试验证（模拟哈希碰撞场景）
```rust
#[test]
fn test_hash_collision_handling() {
    let mut index = ParagraphIndex::new();
    // 注入相同哈希的不同内容（需 mock）
    // 验证两个内容都被正确索引且不覆盖
}
```

**通过标准**: ✅ 模拟碰撞场景时所有内容都被正确追踪

---

#### 🟠 RT-05: 验证 BUG-006 修复（文件元数据）

**修复文件**: 
- `src-tauri/src/duplicate_resolver.rs`: 添加 `files_metadata` 字段
- `src-tauri/src/lib.rs`: `scan_files` 返回元数据

**验证步骤**:
```bash
# 1. 导入文件并分析
# 2. 检查 API 返回的 JSON 包含 files_metadata 数组
# 3. 确认每个文件有 file_name, file_size, modified 字段
```

**通过标准**: ✅ API 响应中包含文件大小和修改时间信息

---

#### 🟠 RT-06: 验证 BUG-007 修复（Set 序列化）

**修复文件**: `src/store/useStore.ts`
**修改位置**: 状态管理 + 序列化/反序列化

**验证方法**: 
```typescript
// 1. 验证 Set 操作正常
const store = useStore.getState();
store.toggleGroupCheck('grp_1');
expect(store.checkedHashes.size).toBe(1);

// 2. 验证序列化-反序列化
const serialized = JSON.stringify({ checkedHashes: Array.from(store.checkedHashes) });
const deserialized = new Set(JSON.parse(serialized).checkedHashes);
expect(deserialized.has('grp_1_hash')).toBe(true);
```

**通过标准**: ✅ Set 类型可被正确序列化和反序列化

---

#### 🟠 RT-07: 验证 BUG-008 修复（超时控制）

**修复文件**: `src/App.tsx`
**修改位置**: `handleFilesSelected` 函数
**修复要求**: 添加 `Promise.race` 超时

**验证步骤**:
```bash
# 1. 导入超大文件或模拟后端挂起
# 2. 确认 60 秒后显示"分析超时"错误提示
# 3. 确认 UI 从 loading 状态恢复
```

**通过标准**: ✅ 超时后正确显示错误、状态恢复

---

#### 🟠 RT-08: 验证 BUG-009 修复（导出竞态）

**修复文件**: `src/components/ExportButton.tsx`
**修改位置**: 导出逻辑
**修复要求**: 导出开始时快照 checkedHashes

**验证步骤**:
```bash
# 1. 分析文件后，勾选部分重复组
# 2. 点击"导出"
# 3. 在保存对话框弹出前，快速取消/勾选其他组
# 4. 导出完成后对比文件内容
```

**通过标准**: ✅ 导出的内容与点击导出时的预览一致，不受后续修改影响

---

#### 🟠 RT-09: 验证 BUG-010 修复（文件验证）

**修复文件**: `src/components/FileDropZone.tsx`
**修改位置**: 文件验证逻辑
**修复要求**: 添加大小检查 + MIME 类型验证

**验证步骤**:
```bash
# 1. 准备一个 >100MB 的伪装 .txt 文件
# 2. 尝试导入
# 3. 确认显示"文件过大"提示且被拒绝
```

**通过标准**: ✅ 过大文件被拒绝并显示合理提示

---

### 阶段 3: 全链路冒烟测试 (所有修复完成后)

---

#### SM-01: 基本功能冒烟

| 步骤 | 操作 | 预期 |
| :--- | :--- | :--- |
| 1 | 启动应用 | 窗口正常显示，标题"文档终版确定器" |
| 2 | 拖入 2 个含重复内容的 .txt 文件 | 加载动画后，左侧显示重复组 |
| 3 | 勾选一个重复组 | 右侧预览立即移除对应段落 |
| 4 | 取消勾选 | 段落恢复显示 |
| 5 | 悬停段落 | 显示来源文件浮层 |
| 6 | 点击导出 | 系统保存对话框弹出 |
| 7 | 保存文件 | 文件内容与预览一致 |

#### SM-02: 多文件 + 中文场景

| 步骤 | 操作 | 预期 |
| :--- | :--- | :--- |
| 1 | 拖入 3 个中文 .txt 文件 | 正常分析，无崩溃 |
| 2 | 检查左侧列表 | 中文 snippet 无截断乱码 |
| 3 | 勾选部分组 | 预览实时更新 |

#### SM-03: 边界场景

| 步骤 | 操作 | 预期 |
| :--- | :--- | :--- |
| 1 | 拖入空文件 | 不崩溃，显示无内容 |
| 2 | 拖入 GBK 编码文件 | 正确显示中文 |
| 3 | 快速连续点击"重新开始" | 状态一致，无闪烁 |
| 4 | 窗口缩放到最小尺寸 | 布局完整 |

---

## 二、回归测试判定矩阵

修复完成后，根据以下矩阵判断**是否可以继续修复 Bug**（即是否进入下一轮迭代）：

```
                    ┌─────────────────────────────────────┐
                    │      回归测试结果                     │
                    │                                     │
                    │  Critical: 0 Fail                    │
                    │  Major:    0 Fail                    │
                    │  SM-01~03: All Pass                  │
                    │                                     │
                    └──────────────┬──────────────────────┘
                                   │
                   ┌───────────────┴───────────────┐
                   │                               │
                   ▼                               ▼
            ✅ 全部通过                     ❌ 存在失败
            ────────────                   ────────────
            进入下一阶段：                  进入修复循环：
            • 部署发布                      • 修复失败项
            • 或开始 V1.1 开发              • 重新执行回归测试
                                           • 重复直到全部通过
```

### 停止修复的条件

以下任一条件满足时**建议暂停修复**并重新评估：

| 条件 | 说明 |
| :--- | :--- |
| 🔴 3 轮迭代后仍有 Critical Bug | 架构层面需要重新设计 |
| 🟠 5 轮迭代后仍有 Major Bug | 实现方案需重新评估 |
| 📉 修复引入新 Bug 比例 > 30% | 修复方案本身有问题 |
| ⏱️ 修复时间超过预期 2 倍 | 范围蔓延，需重新规划 |

---

## 三、回归测试执行检查表

```markdown
## 回归测试执行记录

### 修复轮次: 第 ___ 轮
### 测试日期: _______________
### 测试人员: _______________

### 阶段 1: Critical 修复验证
- [ ] RT-01 BUG-001 字符串切片修复
- [ ] RT-02 BUG-002 编码降级修复  
- [ ] RT-03 BUG-003 预览顺序修复

### 阶段 2: Major 修复验证
- [ ] RT-04 BUG-004 哈希碰撞修复
- [ ] RT-05 BUG-006 文件元数据
- [ ] RT-06 BUG-007 Set 序列化
- [ ] RT-07 BUG-008 超时控制
- [ ] RT-08 BUG-009 导出竞态
- [ ] RT-09 BUG-010 文件验证

### 阶段 3: 全链路冒烟
- [ ] SM-01 基本功能冒烟
- [ ] SM-02 多文件中文场景
- [ ] SM-03 边界场景

### 判定结果
- [ ] ✅ 全部通过 → 进入下一阶段
- [ ] ❌ 存在失败 → 进入第 ___ 轮修复
```

---

## 四、附录: 修复后的构建与部署

### 构建命令

```bash
# 前端构建
cd g:\CodeProject\Text Unifier
npm run build

# Rust 后端构建 (Release)
cd src-tauri
cargo build --release

# 打包部署
# 从 src-tauri\target\release\text-unifier.exe 复制到 deploy\
```

### 快速验证脚本

```powershell
# 快速验证脚本: quick_verify.ps1
Write-Host "=== Text Unifier V1.0 快速回归验证 ==="

Write-Host "[1/3] 创建测试文件..."
"段落A`n段落B`n段落C" | Out-File -Encoding utf8 test_a.txt
"段落A`n段落D`n段落E" | Out-File -Encoding utf8 test_b.txt

Write-Host "[2/3] 启动应用..."
Start-Process ".\deploy\TextUnifier.exe"

Write-Host "[3/3] 手动验证项:"
Write-Host "  1. 拖入 test_a.txt 和 test_b.txt"
Write-Host "  2. 确认 '段落A' 在重复列表中 (出现2次)"
Write-Host "  3. 勾选 '段落A' → 预览移除该段"
Write-Host "  4. 取消勾选 → 预览恢复"
Write-Host "  5. 导出 → 内容正确"
Write-Host "=== 验证完成 ==="
```

---

*指令由 AI 测试验证系统自动生成*
