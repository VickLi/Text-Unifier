# 代码变更记录 V3.1.2

| 项目 | 内容 |
| :--- | :--- |
| **项目名称** | 文档终版确定器（Text Unifier） |
| **版本号** | V3.1.2 |
| **基线版本** | V3.1.1 |
| **变更类型** | 代码规范校验 + camelCase 字段名对齐 + 性能优化 |
| **发布日期** | 2026-05-11 |

---

## 变更总览

| 指标 | 数值 |
| :--- | :---: |
| **修复 Bug 数** | 3 |
| **代码质量优化** | 2 |
| **修改文件数** | 6 |
| **Rust 测试** | **25/25 ✅ 零 warning 减少** |

---

## 文件级变更

### 1. `src/types/index.ts`
| 变更 | 说明 |
| :--- | :--- |
| `ExportResult.saved_path` → `savedPath` | BUG-V3.1.2-001：对齐 napi-rs 自动 camelCase 转换 |

### 2. `src/components/ExportButton.tsx`
| 变更 | 说明 |
| :--- | :--- |
| `result.saved_path` → `result.savedPath` | BUG-V3.1.2-001：运行时字段名对齐 |

### 3. `src/utils/ipc.ts`
| 变更 | 说明 |
| :--- | :--- |
| `exportFile` 返回类型 `{ saved_path }` → `{ savedPath }` | BUG-V3.1.2-001：类型声明对齐 |

### 4. `electron/preload.d.ts`
| 变更 | 说明 |
| :--- | :--- |
| `exportFile` 返回类型 `{ saved_path }` → `{ savedPath }` | BUG-V3.1.2-001：preload 类型声明对齐 |

### 5. `src/store/useStore.ts`
| 变更 | 说明 |
| :--- | :--- |
| `setAnalysisResult` 参数 `file_name/file_size` → `fileName/fileSize` | BUG-V3.1.2-002：类型参数对齐 napi |
| `splitChapters` `require()` → `await import()` | BUG-V3.1.2-003：ESM 兼容 |
| `reorderChapters` `require()` → `await import()` | BUG-V3.1.2-003：ESM 兼容 |
| `splitChapters/reorderChapters` 签名 `() => void` → `() => Promise<void>` | BUG-V3.1.2-003：异步签名对齐 |
| 新增 `_updateChapterList` | OPT-V3.1.2-001：统一章节列表管理 |

### 6. `native/src/file_processor.rs`
| 变更 | 说明 |
| :--- | :--- |
| `file_name` 添加 `#[allow(dead_code)]` | OPT-V3.1.2-002：消除编译器 warning |

### 7. `native/Cargo.toml`
| 变更 | 说明 |
| :--- | :--- |
| `version = "3.1.1"` → `"3.1.2"` | 版本递增 |

---

## 测试结果

| 测试套件 | 结果 |
| :--- | :---: |
| Rust 单元测试 | **25/25 ✅** |
| TypeScript 类型检查 | **零错误 ✅** |
| Vite 构建 | **成功 ✅** |

---

*记录生成日期：2026-05-11*
*版本追溯基线：V3.1.1*
