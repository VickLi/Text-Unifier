appId: com.textunifier.app
productName: "Text Unifier"
copyright: "Text Unifier Team"

directories:
  output: release
  buildResources: icons

files:
  - dist/**/*
  - dist-electron/**/*
  - 03-code/electron/preload.d.ts
  - 03-code/native/*.node
  - 03-code/native/index.js

buildResources: 05-deployment/icons

win:
  target:
    - target: nsis
      arch: [x64]
    - target: portable
      arch: [x64]
  sign: false

electronDist: node_modules/electron/dist

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true

portable:
  artifactName: "TextUnifier_Portable_${version}.exe"
  unpackDirName: "TextUnifier_Portable_${version}"

extraResources:
  - from: 03-code/native
    to: native
    filter:
      - "*.node"
      - "index.js"
      - "package.json"
