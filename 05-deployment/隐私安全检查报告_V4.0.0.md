# Text Unifier — 隐私安全检查报告 V4.0.0

> **扫描日期**: 2026-05-22
> **扫描范围**: 全项目 Git 跟踪文件（排除 node_modules/, dist/, dist-electron/, release/, releases/, native/target/, .venv/）
> **扫描工具**: 自动化 grep + 人工复核
> **版本**: V4.0.0

---

## 一、扫描结果总览

| 检查项 | 结果 | 说明 |
|:------|:---:|:------|
| 硬编码密码/密钥 | ✅ 通过 | 未发现 `password=`, `apiKey=`, `token=` 等硬编码凭证 |
| 私钥/证书文件 | ✅ 通过 | 未发现 `.pem`, `.key`, `.p12`, `.crt` 等密钥文件 |
| 内网 IP 地址 | ✅ 通过 | 未发现 `192.168.x.x`, `10.x.x.x` 等内网地址 |
| 代理凭证泄露 | ✅ 通过 | 未发现带凭证的代理配置 |
| .env 配置文件 | ✅ 通过 | 未发现 `.env` 或 `*.local` 敏感环境变量文件被跟踪 |
| 邮箱地址泄露 | ✅ 通过 | 无个人邮箱硬编码 |
| 本地绝对路径泄露 | ⚠️ 需关注 | 脚本中有 `G:\CodeProject\Text Unifier` 路径（预期内，非公开提交） |
| API 令牌泄露 | ✅ 通过 | 未发现 `ghp_*`, `sk-*`, `AKIA*` 等令牌模式 |
| 数据库凭证 | ✅ 通过 | 无数据库连接字符串或密码 |
| 测试数据隐私 | ✅ 通过 | 无敏感测试文本内容 |
| 开发环境信息 | ⚠️ 低风险 | `scripts/启动开发环境.ps1` 含 MSVC/SDK 版本号 |

---

## 二、详细检查项

### 2.1 源代码检查（src/）

| 文件 | 检查结果 |
|:----|:---------|
| `src/store/useStore.ts` | ✅ 无敏感信息，仅业务逻辑 |
| `src/utils/ipc.ts` | ✅ 无敏感信息，仅 IPC 封装 |
| `src/utils/diffUtils.ts` | ✅ 无敏感信息，仅 diff 算法 |
| `src/components/*.tsx` | ✅ 无敏感信息，仅 UI 渲染 |
| `src/types/index.ts` | ✅ 无敏感信息，仅类型定义 |

### 2.2 Electron 主进程检查（electron/）

| 文件 | 检查结果 |
|:----|:---------|
| `electron/main.ts` | ✅ 无硬编码密钥/密码，仅 IPC handler + 窗口管理 |
| `electron/preload.ts` | ✅ 无敏感信息，仅 contextBridge 暴露 |
| `electron/preload.d.ts` | ✅ 无敏感信息，仅类型声明 |

### 2.3 Rust 原生模块检查（native/）

| 文件 | 检查结果 |
|:----|:---------|
| `native/src/lib.rs` | ✅ 仅 napi 函数导出 |
| `native/src/text_normalizer.rs` | ✅ 仅文本归一化算法 |
| `native/src/chain_merger.rs` | ✅ 仅链式合并算法 |
| `native/src/encoding_detector.rs` | ✅ 仅编码探测逻辑 |
| `native/Cargo.toml` | ✅ 仅依赖声明 |
| `native/Cargo.lock` | ✅ 自动生成，含公共 crate 仓库 URL（可预期） |

### 2.4 脚本文件检查（scripts/）

| 文件 | 发现 | 风险等级 |
|:----|:-----|:--------:|
| `scripts/启动开发环境.ps1` | `G:\CodeProject\Text Unifier` 绝对路径；MSVC `14.44.35207`、SDK `10.0.26100.0` 版本号 | 🟢 低风险 |
| `scripts/rename-electron-output.cjs` | ✅ 无敏感信息 | ✅ |
| `scripts/debug-asar.cjs` | ✅ 无敏感信息 | ✅ |
| `scripts/update-asar.cjs` | ✅ 无敏感信息 | ✅ |
| `scripts/build-git-history.ps1` | 含 `dev@example.com`（已替换占位符） | ✅ 安全 |
| `scripts/rebuild-git-history.ps1` | 含 `dev@example.com`（已替换占位符） | ✅ 安全 |

### 2.5 文档检查（01-requirements/, 02-architecture/, 03-code/, 04-test/, docs/）

| 目录 | 检查结果 |
|:----|:---------|
| `01-requirements/` | ✅ 无敏感信息，仅需求文档 |
| `02-architecture/` | ✅ 无敏感信息，仅架构设计文档 |
| `03-code/` | ✅ 无敏感信息，仅代码 |
| `04-test/` | ✅ 无敏感信息，仅测试报告 |
| `docs/` | ✅ 无敏感信息，仅历史文档 |

### 2.6 配置文件检查

| 文件 | 检查结果 |
|:----|:---------|
| `package.json` | ✅ `"private": true` 已设置，无敏感信息 |
| `.gitignore` | ✅ 配置完整，覆盖 `node_modules/`, `dist/`, `release/`, `*.exe`, `*.zip`, `native/target/` 等 |
| `electron-builder.yml` | ✅ 无敏感信息，仅打包配置 |
| `vite.config.ts` | ✅ 无敏感信息 |
| `tailwind.config.js` | ✅ 无敏感信息 |
| `postcss.config.js` | ✅ 无敏感信息 |
| `tsconfig.json` | ✅ 无敏感信息 |
| `tsconfig.electron.json` | ✅ 无敏感信息 |
| `tsconfig.node.json` | ✅ 无敏感信息 |

---

## 三、已发现的低风险项

### 3.1 scripts/启动开发环境.ps1 — 绝对路径

| 属性 | 说明 |
|:-----|:------|
| **文件** | `scripts/启动开发环境.ps1` |
| **内容** | `cd G:\CodeProject\Text Unifier` |
| **风险等级** | 🟢 低风险 |
| **说明** | 该脚本为本地开发环境专用，不随公开仓库分发。如果未来公开，建议替换为 `$PSScriptRoot` 相对路径。 |
| **修复建议** | 将 `cd G:\CodeProject\Text Unifier` 替换为 `cd (Split-Path $PSScriptRoot)` |

### 3.2 scripts/启动开发环境.ps1 — 工具链版本

| 属性 | 说明 |
|:-----|:------|
| **文件** | `scripts/启动开发环境.ps1` |
| **内容** | MSVC `14.44.35207`、Windows SDK `10.0.26100.0` |
| **风险等级** | 🟢 低风险 |
| **说明** | 暴露了编译工具链的具体版本号，风险极低。 |
| **修复建议** | 使用 `Get-ChildItem` 动态查找最新版 MSVC/SDK |

---

## 四、安全评分

```
隐私安全评分: 98/100 🟢
  ├─ 硬编码凭证: 100% (通过)
  ├─ 个人信息保护: 100% (无邮箱/姓名泄露)
  ├─ 路径泄露防护: 95% (脚本绝对路径可优化)
  ├─ 测试数据清理: 100% (无敏感文本)
  └─ 配置安全: 95% (SDK 版本可动态检测)
```

---

## 五、修复建议汇总

| 编号 | 问题 | 文件 | 建议操作 | 优先级 |
|:----|:-----|:----|:---------|:------:|
| R01 | 绝对路径 `G:\CodeProject\Text Unifier` | `scripts/启动开发环境.ps1` | 替换为 `Split-Path $PSScriptRoot` | 🟢 低 |
| R02 | 硬编码 MSVC/SDK 版本 | `scripts/启动开发环境.ps1` | 动态检测最新版本 | 🟢 低 |

---

*本报告由自动化扫描工具生成，结合人工复核。建议每次发布前重新执行扫描。*
