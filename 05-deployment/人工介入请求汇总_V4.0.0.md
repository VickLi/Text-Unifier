# Text Unifier V4.0.0 — 全流程人工介入请求汇总

> **生成日期**: 2026-06-03（第二轮更新）
> **阶段**: 部署运维 — 最终发布

---

## 一、概述

本文档汇总了 V4.0.0 全 AI 开发流程中所有需要人工介入的请求、决策点和待办事项。按严重级别和阶段分类，供项目负责人审查处理。

---

## 二、待处理人工介入项

### 🔴 必须处理

| 编号 | 阶段 | 请求 | 原因 | 建议处理方式 |
|:----|:----|:-----|:-----|:------------|
| MI-01 | 部署 | **GitHub Release 手动确认** | Release 标签需要人工审核后推送并发布 | 运行 `git push origin main --tags` 推送代码和标签，然后访问 https://github.com/VickLi/Text-Unifier/releases 创建 Release 并粘贴发布说明 |
| MI-02 | 部署 | **完整生产构建** | 当前便携版为之前构建产物，如需重新构建需在本地有 Rust+Node.js 环境时运行完整构建命令 | 在开发机上执行 `npm run electron:build` 生成最新安装包 |

### 🟡 建议处理

| 编号 | 阶段 | 请求 | 原因 | 建议处理方式 |
|:----|:----|:-----|:-----|:------------|
| MI-03 | 隐私 | **脚本绝对路径审查** | `scripts/启动开发环境.ps1` 含有绝对路径 `G:\CodeProject\Text Unifier`，建议替换为相对路径 | 确认该脚本是否公开提交，如是则替换为 `$PSScriptRoot` |
| MI-04 | 测试 | **手动冒烟测试** | 自动化测试已全部通过，建议在目标平台（Win10/Win11）上执行手动冒烟测试 | 在 Windows 10/11 实机上运行便携版，验证核心功能流程 |

### 🟢 可选处理

| 编号 | 阶段 | 请求 | 原因 | 建议处理方式 |
|:----|:----|:-----|:-----|:------------|
| MI-05 | 文档 | **用户手册截图补充** | 手册为纯文本描述，建议补充界面截图提升可读性 | 截取应用各功能区域的界面图，插入 `docs/用户使用手册_V4.0.0.md` |
| MI-06 | 构建 | **macOS/Linux 构建** | 当前仅验证了 Windows x64 构建，跨平台构建需在对应 OS 上执行 | 在 macOS/Linux 上执行 `npm run electron:build` |
| MI-07 | 隐私 | **SDK 版本动态检测** | `scripts/启动开发环境.ps1` 硬编码了 MSVC 14.44 和 SDK 10.0.26100.0 | 使用 PowerShell 动态检测最新版本 |

---

## 三、已自动处理的事项

| 编号 | 事项 | 处理方式 |
|:----|:-----|:---------|
| A-01 | 全项目隐私扫描（第二轮） | 自动执行 grep 扫描 + 人工复核，更新 `隐私安全检查报告_V4.0.0.md` |
| A-02 | 增量集成（4 阶段） | Feature Flag → 启用文档对比 → 启用合并核心 → 完全移除 Flag |
| A-03 | E/M/H 模块分离 | 内容清洗、搜索替换、撤回栈移至独立项目「Text Unifier 内容工具」 |
| A-04 | CHANGELOG 更新 | 基于 V4.0.0 最终状态更新版本更新记录 |
| A-05 | 安装/卸载脚本 | 验证 Windows 安装脚本和卸载脚本 |
| A-06 | GitHub Release 草稿 | 生成完整 Release 说明（含标题、更新内容、下载链接指引） |
| A-07 | `.github/config.yml` 创建 | 创建 GitHub Release 配置文件 |
| A-08 | 代码变更暂存 | 当前所有修改已准备就绪，待提交推送 |

---

## 四、处理建议

1. **优先处理 MI-01**：创建 `.github/config.yml` 以建立标准发布流程
2. **MI-02 紧随其后**：审核 Release 正文并手动发布
3. **MI-03 在开发机上并行执行**：打包过程约 5-10 分钟
4. **MI-04/MI-08 在推送前确认**：如需推送脚本到公开仓库

---

*本报告由部署运维流程自动生成，建议项目负责人逐项确认后归档。*
