# Text Unifier V4.0.0 — 全流程人工介入请求汇总

> **生成日期**: 2026-05-22
> **阶段**: 部署运维

---

## 一、概述

本文档汇总了 V4.0.0 全 AI 开发流程中所有需要人工介入的请求、决策点和待办事项。按严重级别和阶段分类，供项目负责人审查处理。

---

## 二、待处理人工介入项

### 🔴 必须处理

| 编号 | 阶段 | 请求 | 原因 | 建议处理方式 |
|:----|:----|:-----|:-----|:------------|
| MI-01 | 部署 | **GitHub 配置文件缺失** | 未找到 `.github/config.yml` 配置文件，无法自动完成 CI/CD 配置 | 手动创建 `.github/config.yml`，配置 Release 自动发布流程；或手动在 GitHub 网页端创建 Release |
| MI-02 | 部署 | **GitHub Release 手动确认** | Release 标签 `v4.0.0` 已创建并推送，但 Release 正文需人工审核后发布 | 访问 https://github.com/VickLi/Text-Unifier/releases 审核并发布 |

### 🟡 建议处理

| 编号 | 阶段 | 请求 | 原因 | 建议处理方式 |
|:----|:----|:-----|:-----|:------------|
| MI-03 | 构建 | **生产环境打包需手动执行** | 需在本地有 Rust + Node.js 环境时运行 `npm run electron:build` 生成安装包 | 在开发机上执行完整构建命令后，将产出物放入 `releases/v4.0.0/` |
| MI-04 | 隐私 | **脚本绝对路径审查** | `scripts/启动开发环境.ps1` 含有绝对路径 `G:\CodeProject\Text Unifier`，建议替换为相对路径 | 确认该脚本是否公开提交，如是则替换为 `$PSScriptRoot` |
| MI-05 | 测试 | **手动冒烟测试** | 自动化测试已全部通过，建议在目标平台（Win10/Win11）上执行手动冒烟测试 | 在 Windows 10/11 实机上运行便携版，验证核心功能流程 |

### 🟢 可选处理

| 编号 | 阶段 | 请求 | 原因 | 建议处理方式 |
|:----|:----|:-----|:-----|:------------|
| MI-06 | 文档 | **用户手册截图补充** | 手册为纯文本描述，建议补充界面截图提升可读性 | 截取应用各功能区域的界面图，插入 `docs/用户使用手册_V4.0.0.md` |
| MI-07 | 构建 | **macOS/Linux 构建** | 当前仅验证了 Windows x64 构建，跨平台构建需在对应 OS 上执行 | 在 macOS/Linux 上执行 `npm run electron:build` |
| MI-08 | 隐私 | **SDK 版本动态检测** | `scripts/启动开发环境.ps1` 硬编码了 MSVC 14.44 和 SDK 10.0.26100.0 | 使用 PowerShell 动态检测最新版本 |

---

## 三、已自动处理的事项

| 编号 | 事项 | 处理方式 |
|:----|:-----|:---------|
| A-01 | 全项目隐私扫描 | 自动执行 grep 扫描 + 人工复核，生成 `隐私安全检查报告_V4.0.0.md` |
| A-02 | 代码推送 + 标签 | 自动创建 `v4.0.0` Git 标签并推送至远程仓库 |
| A-03 | CHANGELOG 生成 | 基于 V4.0.0 所有变更自动生成版本更新记录 |
| A-04 | 安装/卸载脚本 | 生成 Windows 安装脚本和卸载脚本 |
| A-05 | 用户手册 | 生成完整用户使用手册 |
| A-06 | 文档归档方案 | 制定全项目归档包结构说明 |
| A-07 | GitHub Release 草稿 | 生成完整 Release 说明（含标题、更新内容、下载链接指引） |

---

## 四、处理建议

1. **优先处理 MI-01**：创建 `.github/config.yml` 以建立标准发布流程
2. **MI-02 紧随其后**：审核 Release 正文并手动发布
3. **MI-03 在开发机上并行执行**：打包过程约 5-10 分钟
4. **MI-04/MI-08 在推送前确认**：如需推送脚本到公开仓库

---

*本报告由部署运维流程自动生成，建议项目负责人逐项确认后归档。*
