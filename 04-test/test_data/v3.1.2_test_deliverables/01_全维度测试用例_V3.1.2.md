# Text Unifier V3.1.2 全维度测试用例（初测版）

| 项目 | 内容 |
| :--- | :--- |
| **应用名称** | 文档终版确定器（Text Unifier） |
| **版本号** | V3.1.2 |
| **测试日期** | 2026-05-11 |
| **测试范围** | camelCase 字段名对齐、ESM `require()`→`import()`、Rust warning 消除 |
| **变更类型** | 代码规范校验 + 兼容性修复 + 性能优化 |

---

## 目录

1. [修复验证测试（REG）](#1-修复验证测试reg)
2. [功能测试（FUNC）](#2-功能测试func)
3. [接口测试（API）](#3-接口测试api)
4. [性能测试（PERF）](#4-性能测试perf)
5. [安全测试（SEC）](#5-安全测试sec)
6. [兼容性测试（COMPAT）](#6-兼容性测试compat)
7. [边界与异常测试（BOUND）](#7-边界与异常测试bound)

---

## 1. 修复验证测试（REG）

### 1.1 BUG-V3.1.2-001：`ExportResult.savedPath` 字段名对齐

| 编号 | 测试场景 | 前置条件 | 测试步骤 | 预期结果 | ✅ |
| :--- | :--- | :--- | :--- | :--- | :---: |
| REG-001-01 | 导出后字段名正确 | 分析完成，有可导出的段落 | 1. 点击「导出」<br>2. 选择保存位置<br>3. 检查返回结果 | 返回 `{ savedPath: string }`（非 `saved_path`） | ☐ |
| REG-001-02 | Toast 正常显示路径 | 导出成功 | 观察界面右下角 Toast | 显示「导出成功！已保存至: C:\...」 | ☐ |
| REG-001-03 | TypeScript 类型无报错 | — | `npx tsc --noEmit` | 零错误 | ☐ |
| REG-001-04 | preload.d.ts 类型声明对齐 | — | 检查 `electron/preload.d.ts` | `exportFile` 返回 `{ savedPath: string }` | ☐ |

### 1.2 BUG-V3.1.2-002：`setAnalysisResult` 参数 camelCase

| 编号 | 测试场景 | 前置条件 | 测试步骤 | 预期结果 | ✅ |
| :--- | :--- | :--- | :--- | :--- | :---: |
| REG-002-01 | 接口签名对齐 | — | 检查 `useStore.ts` 中 `setAnalysisResult` | 参数类型 `{ fileName: string; fileSize: number; modified: number }[]` | ☐ |
| REG-002-02 | 编译通过 | — | `npx tsc --noEmit` | 零错误 | ☐ |
| REG-002-03 | 运行时调用 | 导入文件分析 | 分析完成后查看 Store | 正常返回，无字段 undefined | ☐ |

### 1.3 BUG-V3.1.2-003：`require()` → `await import()` ESM 兼容

| 编号 | 测试场景 | 前置条件 | 测试步骤 | 预期结果 | ✅ |
| :--- | :--- | :--- | :--- | :--- | :---: |
| REG-003-01 | splitChapters 使用 ESM import | 含内联章节文本 | 1. 点击「章节分割」 | 分割完成，不报错 | ☐ |
| REG-003-02 | reorderChapters 使用 ESM import | 乱序章节文本 | 1. 点击「章节重排」 | 重排完成，不报错 | ☐ |
| REG-003-03 | applyProcessing 使用 ESM import | 正常文本 | 1. 点击「应用处理」 | 处理完成，不报错 | ☐ |
| REG-003-04 | 函数签名正确 | — | `npx tsc --noEmit` | `splitChapters`/`reorderChapters` 签名 `() => Promise<void>` | ☐ |
| REG-003-05 | Vite 构建无 CJS warning | — | `npm run build` | 构建成功，无 `require()` 相关 warning | ☐ |

### 1.4 OPT-V3.1.2-001：`_updateChapterList` 统一管理

| 编号 | 测试场景 | 前置条件 | 测试步骤 | 预期结果 | ✅ |
| :--- | :--- | :--- | :--- | :--- | :---: |
| REG-004-01 | 统一函数存在 | — | 检查 Store | `_updateChapterList: () => void` 存在于 store 中 | ☐ |
| REG-004-02 | applyProcessing 后章节列表正确 | 含章节文本 | 应用处理 | chapterList 正确反映当前段落 | ☐ |

### 1.5 OPT-V3.1.2-002：Rust warning 消除

| 编号 | 测试场景 | 前置条件 | 测试步骤 | 预期结果 | ✅ |
| :--- | :--- | :--- | :--- | :--- | :---: |
| REG-005-01 | cargo clippy 无 warning | — | `cargo clippy` | 零 warning | ☐ |
| REG-005-02 | cargo test 全部通过 | — | `cargo test` | 25/25 全部通过 | ☐ |
| REG-005-03 | file_processor 编译无 dead_code | — | 检查 `file_processor.rs` | `#[allow(dead_code)]` 已添加 | ☐ |

---

## 2. 功能测试（FUNC）

> V3.1.1 全部 ~46 个 FUNC 用例保留。无新增功能变更。

| 编号 | 回归场景 | 变更说明 | ✅ |
| :--- | :--- | :--- | :---: |
| FUNC-REG-01 | 导出文件 | 字段名对齐后导出功能不受影响 | ☐ |
| FUNC-REG-02 | 章节分割（独立操作） | 使用 ESM import，功能不变 | ☐ |
| FUNC-REG-03 | 章节重排（独立操作） | 使用 ESM import，功能不变 | ☐ |
| FUNC-REG-04 | 应用处理全流水线 | `setAnalysisResult` 参数对齐不影响运行 | ☐ |

---

## 3. 接口测试（API）

| 编号 | 接口 | 测试场景 | 预期 | ✅ |
| :--- | :--- | :--- | :--- | :---: |
| API-01 | exportFile 返回类型 | 导出成功 | 返回 `{ savedPath: string }` | ☐ |
| API-02 | exportFile preload 类型 | 编译检查 | `electron/preload.d.ts` 类型声明一致 | ☐ |
| API-03 | splitChapters ESM | 动态 import | 正常加载 novelProcessor 函数 | ☐ |
| API-04 | reorderChapters ESM | 动态 import | 正常加载 novelProcessor 函数 | ☐ |
| API-05 | cargo test | 全量 Rust 测试 | 25/25 通过 | ☐ |
| API-06 | tsc --noEmit | 全量 TS 检查 | 零错误 | ☐ |
| API-07 | npm run build | 全量构建 | 成功 | ☐ |

---

## 4. 性能测试（PERF）

| 编号 | 测试场景 | 说明 | ✅ |
| :--- | :--- | :--- | :---: |
| PERF-01 | splitChapters 首次调用 | ESM dynamic import 首次加载耗时 | ☐ |
| PERF-02 | reorderChapters 首次调用 | ESM dynamic import 首次加载耗时 | ☐ |
| PERF-03 | 连续多次章节操作 | 第 2+ 次调用缓存生效，无额外开销 | ☐ |

---

## 5. 安全测试（SEC）

| 编号 | 测试场景 | 说明 | ✅ |
| :--- | :--- | :--- | :---: |
| SEC-01 | ESM import 路径安全 | `await import('../utils/novelProcessor')` — 硬编码路径，无注入 | ☐ |
| SEC-02 | 导出路径字段变更 | `savedPath` 仅用于显示，不执行额外操作 | ☐ |

---

## 6. 兼容性测试（COMPAT）

| 编号 | 测试场景 | 说明 | ✅ |
| :--- | :--- | :--- | :---: |
| COMPAT-01 | ESM `await import()` Electron 环境 | Chromium 89+ 原生支持 | ☐ |
| COMPAT-02 | napi-rs camelCase 跨平台 | Win/Mac napi-rs 行为一致 | ☐ |

---

## 7. 边界与异常测试（BOUND）

| 编号 | 测试场景 | 说明 | ✅ |
| :--- | :--- | :--- | :---: |
| BOUND-01 | exportFile 返回 undefined savedPath | 异常情况下字段名对齐仍然有效 | ☐ |
| BOUND-02 | splitChapters 在无内容时调用 | 空文本 → 不崩溃，返回空 | ☐ |
| BOUND-03 | reorderChapters 在无章节时调用 | 抛出 Error → Store 的 catch 处理 | ☐ |

---

## 用例统计

| 测试维度 | 用例数 |
| :--- | :---: |
| 修复验证（REG） | 18 |
| 功能测试（FUNC） | 4 |
| 接口测试（API） | 7 |
| 性能测试（PERF） | 3 |
| 安全测试（SEC） | 2 |
| 兼容性测试（COMPAT） | 2 |
| 边界异常（BOUND） | 3 |
| **小计（V3.1.2 新增）** | **39** |
| V3.1.1 保留用例 | ~80 |
| **总计** | **~120** |
